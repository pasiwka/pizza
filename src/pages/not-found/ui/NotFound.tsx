import React from "react";
import NotFoundBlock from "../model";

const NotFound: React.FC = () => {
  return (
    <div className="container">
      <NotFoundBlock />
    </div>
  );
};

export default NotFound;
